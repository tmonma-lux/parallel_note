.page-title タグ一覧

= render partial: 'shared/search',
        locals: { url: search_tags_path, query: @query, target: 'キーワード', tag_search: false }

= render partial: 'tagtable', locals: { tags: @tags }
